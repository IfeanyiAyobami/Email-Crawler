//Create a function, getFirst(Array), that returns the first item in the array

function getFirst(arr) {
    return arr[o]
}


let firstCard = getFirst([10, 11, 7])

console.log(firstCard)



// Call it with an array as an argument to verify that it works
